export interface ExhibitorNote {
  noteDate: Date;
  followUpDate: Date;
  personResponsible: string;
  notes: string;
  dateCompleted: Date;
  completedBy: string;
  exhibitorId: number;
  noteId: number;
}
