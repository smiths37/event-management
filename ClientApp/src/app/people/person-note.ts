export interface PersonNote {
  noteDate: Date;
  followUpDate: Date;
  personResponsible: string;
  notes: string;
  dateCompleted: Date;
  completedBy: string;
  personId: number;
  noteId: number;
}
